# Deprecated placeholder for the old Java library template name.
# Use java-library-internal-build.yml instead.
name: Java Library Build (Deprecated)

on:
  workflow_dispatch:

# Deprecated: this file existed before the reusable workflow was renamed.
# Use .github/caller-templates/java-library-internal-build.yml instead.

jobs: {}
