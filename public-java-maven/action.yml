name: 'Main Java Library Build for public Maven Releases'
description: 'GitHub Actions composite to build, test, publish and promote a Java library (converted from Jenkins pipeline).'

inputs:
  java_version:
    description: 'The Java version to use.'
    required: true
    default: 'java21'
  is_release:
    description: 'Whether to perform a release build.'
    required: false
    default: false
  version:
    description: 'The new version number for a release (e.g., 1.2.3).'
    required: false
  artifactory_username:
    description: 'The username for Artifactory.'
    required: true
  artifactory_password:
    description: 'The password for Artifactory.'
    required: true
  ks_github_app_id:
    description: 'GitHub App ID for authentication.'
    required: true
  ks_github_app_private_key:
    description: 'GitHub App Private Key for authentication.'
    required: true
  dependency_track_api_key:
    description: 'The API key for Dependency-Track.'
    required: true
  gpg_private_key:
    description: 'GPG private keys for signing artifacts.'
    required: true
  gpg_passphrase:
    description: 'Passphrase for the GPG private key.'
    required: true
  maven_settings_xml:
    description: 'Maven settings.xml for deploying to Maven Central.'
    required: false

runs:
  using: "composite"
  steps:
    - name: 'Set up GitHub App'
      id: app_token
      uses: ks-no/github-actions-public/setup-github-app@main
      with:
        ks_github_app_id: ${{ inputs.ks_github_app_id }}
        ks_github_app_private_key: ${{ inputs.ks_github_app_private_key }}

    - name: Checkout code
      uses: actions/checkout@0c366fd6a839edf440554fa01a7085ccba70ac98 #v6.0.2
      with:
        submodules: true
        fetch-depth: 0
        token: ${{ steps.app_token.outputs.app_token }}
        fetch-tags: true

    - name: Switch Java Version
      uses: actions/setup-java@be666c2fcd27ec809703dec50e508c2fdc7f6654 #v5.2.0
      with:
        distribution: 'temurin'
        java-version: ${{ inputs.java_version }}
        cache: 'maven'

    - name: Prepare JFrog CLI
      uses: ks-no/github-actions-public/prepare-the-frog@main
      with:
        artifactory_username: ${{ inputs.artifactory_username }}
        artifactory_password: ${{ inputs.artifactory_password }}

    - name: Cache Maven packages
      uses: actions/cache@v4
      with:
        path: ~/.m2/repository
        key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
        restore-keys: |
          ${{ runner.os }}-maven-

    - name: Set up Maven
      uses: stCarolas/setup-maven@v5 #Remove when: https://github.com/jfrog/build-info/issues/841 is resolved
      with:
        maven-version: 3.9.11

    - name: Use Maven settings.xml
      shell: bash
      if: inputs.maven_settings_xml != ''
      run: |
        mkdir -p ~/.m2
        echo "${{ inputs.maven_settings_xml }}" | base64 -d > ~/.m2/settings.xml

    - name: Snapshot - Verify POM
      if: inputs.is_release != 'true'
      shell: bash
      run: |
        jfrog mvn -U -B -P snapshot validate

    - name: Release - Set new version and tag
      if: inputs.is_release == 'true' && (startsWith(github.ref, 'refs/heads/main') || startsWith(github.ref, 'refs/heads/master'))
      shell: bash
      run: |
        jfrog mvn versions:set -DnewVersion=${{ inputs.version }} -DgenerateBackupPoms=false
        git commit -am "Release ${{ inputs.version }}"
        git tag -a ${{ inputs.version }} -m "Release ${{ inputs.version }}"

    - name: Release - Verify POM
      if: inputs.is_release == 'true' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
      shell: bash
      run: |
        jfrog mvn -U -B validate

    - name: Build
      shell: bash
      run: |
        if [[ "${{ inputs.is_release }}" == "true" && 
              ( "${GITHUB_REF}" == "refs/heads/main" || "${GITHUB_REF}" == "refs/heads/master" ) ]]; then
          export JFROG_CLI_BUILD_NAME="ks-maven"
        else
          export JFROG_CLI_BUILD_NAME="ks-maven-snapshot"
        fi
        export JFROG_CLI_BUILD_NUMBER="${{ github.run_number }}"
        jfrog mvn -DinstallAtEnd=true -U -B clean install

    - name: Generate SBOM
      shell: bash
      run: |
        mvn org.cyclonedx:cyclonedx-maven-plugin:2.9.1:makeAggregateBom -Dcyclonedx.outputFormat=json -Dcyclonedx.outputName=bom -Dcyclonedx.skipNotDeployed=false

    - name: Upload SBOM to Dependency-Track
      if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') && inputs.is_release == 'true'
      shell: bash
      run: |
        echo "Searching for SBOM files..."
        find . -name "bom.json" -o -name "*bom*.json" | head -20
        
        # Try to find the bom.json file
        BOM_FILE=$(find . -name "bom.json" -type f | head -1)
        
        if [ -z "$BOM_FILE" ]; then
          echo "Error: No bom.json file found in the project!"
          echo "Listing all target directories:"
          find . -type d -name "target" -exec ls -la {} \; | head -50
          exit 1
        fi
        
        echo "Found SBOM file at: $BOM_FILE"
        curl -X POST --header "X-Api-Key: ${{ inputs.dependency_track_api_key }}" \
          -F "projectName=${{ github.event.repository.name }}" \
          -F "autoCreate=true" \
          -F "bom=@$BOM_FILE" \
          https://dt.fiks.dev.ks.no/api/v1/bom
    - name: Deploy to Maven Central
      if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
      shell: bash
      env:
        MAVEN_GPG_PASSPHRASE: ${{ inputs.gpg_passphrase }}
        GNUPGHOME: ${{ runner.temp }}/.gnupg
      run: |
        if [[ -n "${{ inputs.gpg_private_key }}" ]]; then
          mkdir -p $GNUPGHOME
          echo "${{ inputs.gpg_private_key }}" | gpg --batch --import
        fi
        PROFILE=""
        if [[ "${{ inputs.is_release }}" == "true" ]]; then
          PROFILE="-P release"
        fi
        jfrog mvn -B -e $PROFILE -DskipTests -Dmaven.test.skip=true deploy

    - name: Publish Build Info
      shell: bash
      env:
        JFROG_CLI_BUILD_NAME: ${{ github.event.repository.name }}
        JFROG_CLI_BUILD_NUMBER: ${{ github.run_number }}
      run: |
        jfrog rt build-publish

    - name: Promote Release Build
      if: inputs.is_release == 'true' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
      shell: bash
      env:
        JFROG_CLI_BUILD_NAME: ${{ github.event.repository.name }}
        JFROG_CLI_BUILD_NUMBER: ${{ github.run_number }}
      run: |
        echo "Delaying promote to ensure all artifacts are available in Artifactory before promotion."
        sleep 15
        jfrog rt build-promote $JFROG_CLI_BUILD_NAME $JFROG_CLI_BUILD_NUMBER

    - name: Promote Snapshot Build
      if: inputs.is_release != 'true' && !startsWith(github.ref, 'refs/heads/dependabot')
      shell: bash
      env:
        JFROG_CLI_BUILD_NAME: ${{ github.event.repository.name }}
        JFROG_CLI_BUILD_NUMBER: ${{ github.run_number }}
      run: |
        jfrog rt build-promote $JFROG_CLI_BUILD_NAME $JFROG_CLI_BUILD_NUMBER

    - name: Release - Set next snapshot version
      if: inputs.is_release == 'true' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
      shell: bash
      run: |
        IFS='.' read -r major minor patch <<< "${{ inputs.version }}"
        next_patch=$((patch + 1))
        next_snapshot_version="$major.$minor.$next_patch-SNAPSHOT"
        echo "Next snapshot version will be $next_snapshot_version"
        jfrog mvn versions:set -DnewVersion=$next_snapshot_version -DgenerateBackupPoms=false
        git commit -am "Bump to next snapshot version"

    - name: Generate Release Notes
      if: inputs.is_release == 'true' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
      uses: ks-no/github-actions-public/generate-release-notes@main
      id: generate_body
      with:
        github-token: ${{ steps.app_token.outputs.app_token }}
        actor: ${{ github.actor }}
        reviewer: ${{ inputs.reviewer }}

    - name: Create GitHub Release
      if: inputs.is_release == 'true' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
      uses: softprops/action-gh-release@153bb8e04406b158c6c84fc1615b65b24149a1fe #v2.6.1
      env:
        GITHUB_TOKEN: ${{ steps.app_token.outputs.app_token }}
      with:
        tag_name: ${{ inputs.version }}
        name: "Release ${{ inputs.version }}"
        body: ${{ steps.generate_body.outputs.release_body }}
        draft: false
        prerelease: false

    - name: Push changes
      if: inputs.is_release == 'true' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
      shell: bash
      run: |
        git push --follow-tags
